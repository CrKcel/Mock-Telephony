package android.os;

/** Compile-time stub; the boot classpath implementation is used at runtime. */
public final class ServiceManager {
    public static void addService(String name, IBinder service) {
    }
}
